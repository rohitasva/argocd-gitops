apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: catalog-all
  description: Collection of example backstage catalog
spec:
  targets:
    - ./domain.yaml
    - ./group.yaml
    - ./system.yaml
    - ./argocd/all.yaml
