apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: catalog-all
  description: Argocd backstage catalog
spec:
  targets:
    - ./component.yaml
    - ./resource.yaml
    - ./system.yaml

